# auth-with-nodejs-angular

## To run the API
go to backend and do `npm i`
create a PostgreSQL database named **formation-nodejs**
do `npm start`

## To run the GUI
go to frontend and do `npm i`
do `npm start`
